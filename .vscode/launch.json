{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build & Run",
      "type": "node",
      "request": "launch",
      "preLaunchTask": "Build with tsup",
      "program": "${workspaceFolder}/dist/index.js",
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "console": "integratedTerminal"
    }
  ]
}
